formatter: "markdown"

settings:
  anchor: false
  default: false
  hide-empty: true

recursive:
  enabled: false

sort:
  enabled: true
  by: required

output:
  file: "README.md"
  mode: replace
  template: |-
    []: # "BEGIN"
    {{ .Content }}
    []: # "END"